'use strict';
/*
describe('Service: HttpRequest', function() {

    var service,
        $http,
        $rootScope,
        $q,
        SessionData;

    beforeEach(function() {
        module('musicServerApp');

        inject(function($injector) {
            $rootScope = $injector.get('$rootScope');
            $http = $injector.get('$http');
            $q = $injector.get('$q');
            SessionData = $injector.get('SessionData');

            var factory = $injector.get('HttpRequestTwo', {
                $http: $http,
                $q: $q,
                $rootScope: $rootScope,
                SessionData: SessionData
            });
            service = factory.new();
        });
    });

    describe('test', function() {

        it('should ...', function() {
            console.log(service);

            expect(service.test()).toBe('abcdefg');
        });

    });
});
*/
